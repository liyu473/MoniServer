name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            8.0.x
            9.0.x

      - name: Build and Pack LyuMonionCore
        run: |
          dotnet restore MonionCore/LyuMonionCore.csproj
          dotnet build MonionCore/LyuMonionCore.csproj -c Release --no-restore
          dotnet pack MonionCore/LyuMonionCore.csproj -c Release --no-build -o ./artifacts

      - name: Build and Pack LyuMonion.JwtAuth
        run: |
          dotnet restore LyuMonion.JwtAuth/LyuMonion.JwtAuth.csproj
          dotnet build LyuMonion.JwtAuth/LyuMonion.JwtAuth.csproj -c Release --no-restore
          dotnet pack LyuMonion.JwtAuth/LyuMonion.JwtAuth.csproj -c Release --no-build -o ./artifacts

      - name: List artifacts
        run: ls -la ./artifacts/

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: nuget-packages
          path: ./artifacts/*.nupkg

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: ./artifacts/*.nupkg
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
