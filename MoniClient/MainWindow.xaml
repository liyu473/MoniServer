<Window
    x:Class="MoniClient.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
    xmlns:local="clr-namespace:MoniClient"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    Title="MainWindow"
    Width="800"
    Height="450"
    d:DataContext="{d:DesignInstance Type=local:MainWindowViewModel}"
    ui:TitleBar.Height="36"
    ui:WindowHelper.SystemBackdropType="Mica"
    ui:WindowHelper.UseModernWindowStyle="True"
    mc:Ignorable="d">
    <ikw:SimpleStackPanel VerticalAlignment="Center" Spacing="5">
        <TextBlock
            FontSize="24"
            FontWeight="Bold"
            Text="Test MagicOnion"
            TextAlignment="Center" />

        <Button
            Margin="0,20,0,0"
            HorizontalAlignment="Center"
            Command="{Binding CalculatorCommand}"
            Content="Calculaor" />
        <Button
            Margin="0,20,0,0"
            HorizontalAlignment="Center"
            Command="{Binding SayHelloCommand}"
            Content="SayHello" />
        <Button
            Margin="0,20,0,0"
            HorizontalAlignment="Center"
            Command="{Binding GetPersonCommand}"
            Content="GetPerson" />

        <ikw:SimpleStackPanel
            Margin="20"
            HorizontalAlignment="Center"
            Orientation="Horizontal"
            Spacing="20">
            <Button
                HorizontalAlignment="Center"
                Command="{Binding JoinRoomCommand}"
                Content="加入房间" />
            <Button Command="{Binding LeaveRoomCommand}" Content="离开房间" />
        </ikw:SimpleStackPanel>

        <ikw:SimpleStackPanel
            Margin="20"
            HorizontalAlignment="Center"
            Orientation="Horizontal"
            Spacing="20">
            <Button
                HorizontalAlignment="Center"
                Command="{Binding ServerSendForAllCommand}"
                Content="让服务器广播消息" />
            <Button Command="{Binding ServerSendForSingleCommand}" Content="让服务器指定此客户端发送" />
        </ikw:SimpleStackPanel>

        <Button
            Margin="0,20,0,0"
            HorizontalAlignment="Center"
            Command="{Binding SendPersonCommand}"
            Content="广播Person类" />
    </ikw:SimpleStackPanel>
</Window>
